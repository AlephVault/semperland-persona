# Semperland Persona
This is a sandbox to configure a Semperland Persona object (which is, essentially, a per-address configuration for Semperland ecosystem: avatar, identity, aesthetics).

# Architecture
This app does **NOT** involve a back-end, but just a front-end (as a sandbox) and a deployed smart contract.
